spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/animals}
    username: ${SPRING_DATASOURCE_USERNAME:animals}
    password: ${SPRING_DATASOURCE_PASSWORD:password}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none                # правильно — Liquibase управляет схемой
    show-sql: ${SPRING_JPA_SHOW_SQL:false}  # true только для отладки (логи забиваются)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  liquibase:
    enabled: ${SPRING_LIQUIBASE_ENABLED:true}
    change-log: classpath:db/changelog/changelog-master.yaml

springdoc:
  api-docs:
    path: /v3/api-docs            # стандартный путь (можно оставить /api-docs)
    swagger-ui:
      path: /swagger-ui.html
      operations-sorter: alpha
      tags-sorter: alpha
      doc-expansion: list